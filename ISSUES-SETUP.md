# Issue 事前登録用（講師向け）

第3回研修の前に、この5件を GitHub の Issue として登録しておいてください。
**当日は5チームがIssue #1〜#5を1件ずつ担当します**。1チーム＝1Issue＝1専用ファイル＝基本1PRです。

⚠️ 登録順が Issue 番号になります。スライドと一致させるため、**この順番のまま**登録してください。
受講者は「担当Issueを開く → 専用ブランチを切る → 専用ファイルだけを変更 → PRで `Closes #N` と紐づける」という流れを体験します。

## 登録方法

**方法A：GitHub の画面から**
Issues タブ → New issue → 下のタイトルと本文をコピペ → Submit

**方法B：コマンドで一括登録（gh コマンド）**
リポジトリのフォルダで、下のコマンドを記載順に実行します。

```bash
gh issue create --title "予約を名前で検索できるようにする" --body "## やりたいこと
予約が増えると目的の予約を探すのが大変。名前で絞り込めるようにしたい。

## 決まっていること
- 予約一覧の上に検索ボックスを置く
- 入力するとその場で絞り込まれる（ボタン不要）
- 予約者の名前で検索できる

## 決まっていないこと（exploreで詰める）
- 会議室名でも検索できるべきか
- 大文字小文字・ひらがなカタカナの扱い
- 検索結果がゼロ件のときの表示

## 触るファイル
features/issue-1-search.js だけ

共通ファイルや他チームのファイルは変更しません。"

gh issue create --title "会議室で絞り込めるようにする" --body "## やりたいこと
「第1会議室の予約だけ見たい」というとき、すぐ絞り込めるようにしたい。

## 決まっていること
- 予約一覧の上に、会議室を選ぶプルダウンを置く
- 「すべて表示」も選べる

## 決まっていないこと（exploreで詰める）
- 検索（#1）と同時に使えるべきか
- 初期状態は「すべて表示」でよいか

## 触るファイル
features/issue-2-room-filter.js だけ

共通ファイルや他チームのファイルは変更しません。"

gh issue create --title "ダークモードを付ける" --body "## やりたいこと
夜間や暗い会議室でも見やすいよう、暗い配色に切り替えられるようにしたい。

## 決まっていること
- ヘッダーに切り替えボタンを置く
- 押すと画面全体が暗い配色になる

## 決まっていないこと（exploreで詰める）
- 次に開いたとき、前回の設定を覚えているべきか
- ボタンの表示はアイコンか文字か

## 触るファイル
features/issue-3-dark-mode.js だけ

共通ファイルや他チームのファイルは変更しません。"

gh issue create --title "予約をキャンセルできるようにする" --body "## やりたいこと
間違えて予約したとき、取り消せるようにしたい。

## 決まっていること
- 各予約カードにキャンセルボタンを置く
- 押すと一覧から消える

## 決まっていないこと（exploreで詰める）
- 「本当に消しますか？」の確認は要るか
- 他人の予約も消せてよいか
- 消した記録は残すのか

## 触るファイル
features/issue-4-cancel.js だけ

共通ファイルや他チームのファイルは変更しません。"

gh issue create --title "今月の予約件数を表示する" --body "## やりたいこと
「今月どのくらい会議室が使われているか」がひと目で分かるようにしたい。

## 決まっていること
- ヘッダー部分に件数を表示する

## 決まっていないこと（exploreで詰める）
- 「今月」の基準（今日を含む月／表示中の月）
- 会議室ごとの内訳も出すか
- 予約を追加・削除したらすぐ変わるべきか

## 触るファイル
features/issue-5-monthly-count.js だけ

共通ファイルや他チームのファイルは変更しません。"
```

## 登録後の確認

Issues タブに5件並んでいればOKです。番号（#1〜#5）が、スライドの記載と一致しているか確認してください。
番号がずれた場合は、スライドの番号を実際のIssue番号に合わせます。

## 当日の流れ（参考）

1. 各チームが担当Issueを開き、Assignees にチームメンバーを設定
2. 指定された `feature/issue-N-...` ブランチを切る
3. 専用featureファイルだけを実装する
4. PR本文に `Closes #1` のように担当Issue番号を書く
5. 別メンバーが「担当ファイルだけ・Issue要件・実画面」の3点をレビュー
6. CI成功後に承認・マージし、Issueが自動で閉じることを確認

---

*i-kigai株式会社 / AI Workshop Series #3*
