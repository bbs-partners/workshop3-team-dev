# Issue 事前登録用（講師向け）

第3回研修の前に、この5件を GitHub の Issue として登録しておいてください。
**当日は #1〜#3 を1人1つずつ担当します**（3人チーム想定）。#4・#5 は余力のあるチーム向けです。

⚠️ 登録順が Issue 番号になります。スライドと一致させるため、**この順番のまま**登録してください。
受講者は「自分の担当Issueを選ぶ → ブランチを切る → PRで `Closes #N` と紐づける」という流れを体験します。

## 登録方法

**方法A：GitHub の画面から**
Issues タブ → New issue → 下のタイトルと本文をコピペ → Submit

**方法B：コマンドで一括登録（gh コマンド）**
リポジトリのフォルダで、下のコマンドをそのまま実行すれば5件まとめて作られます。

```bash
gh issue create --title "予約を名前で検索できるようにする" --body "## やりたいこと
予約が増えると目的の予約を探すのが大変。名前で絞り込めるようにしたい。

## 決まっていること
- 予約一覧の上に検索ボックスを置く
- 入力するとその場で絞り込まれる（ボタン不要）
- 予約者の名前で検索できる

## 決まっていないこと（exploreで詰める）
- 会議室名でも検索できるべきか
- 大文字小文字・ひらがなカタカナの扱い
- 検索結果がゼロ件のときの表示

## 触るファイル
index.html / app.js"

gh issue create --title "会議室で絞り込めるようにする" --body "## やりたいこと
「第1会議室の予約だけ見たい」というとき、すぐ絞り込めるようにしたい。

## 決まっていること
- 予約一覧の上に、会議室を選ぶプルダウンを置く
- 「すべて表示」も選べる

## 決まっていないこと（exploreで詰める）
- 検索（#1）と同時に使えるべきか
- 初期状態は「すべて表示」でよいか

## 触るファイル
index.html / app.js"

gh issue create --title "ダークモードを付ける" --body "## やりたいこと
夜間や暗い会議室でも見やすいよう、暗い配色に切り替えられるようにしたい。

## 決まっていること
- ヘッダーに切り替えボタンを置く
- 押すと画面全体が暗い配色になる

## 決まっていないこと（exploreで詰める）
- 次に開いたとき、前回の設定を覚えているべきか
- ボタンの表示はアイコンか文字か

## 触るファイル
style.css / index.html / app.js"

gh issue create --title "予約をキャンセルできるようにする" --body "## やりたいこと
間違えて予約したとき、取り消せるようにしたい。

## 決まっていること
- 各予約カードにキャンセルボタンを置く
- 押すと一覧から消える

## 決まっていないこと（exploreで詰める）
- 「本当に消しますか？」の確認は要るか
- 他人の予約も消せてよいか
- 消した記録は残すのか

## 触るファイル
app.js"

gh issue create --title "今月の予約件数を表示する" --body "## やりたいこと
「今月どのくらい会議室が使われているか」がひと目で分かるようにしたい。

## 決まっていること
- ヘッダー部分に件数を表示する

## 決まっていないこと（exploreで詰める）
- 「今月」の基準（今日を含む月／表示中の月）
- 会議室ごとの内訳も出すか
- 予約を追加・削除したらすぐ変わるべきか

## 触るファイル
index.html / app.js"
```

## 登録後の確認

Issues タブに5件並んでいればOKです。番号（#1〜#5）が、スライドの記載と一致しているか確認してください。
番号がずれた場合は、スライド「まず、自分の Issue を選ぶ」の番号を実際に合わせて修正してください。

## 当日の流れ（参考）

1. 受講者が Issues タブを開き、#1〜#3 から1つ選んで Assignees に自分を設定
2. `feature/...` ブランチを切って実装
3. PR作成時、本文に `Closes #1` のように自分のIssue番号を書く
4. レビュー・承認・マージ → **Issueが自動で閉じる**
5. 最後に、各自が気づいたことを1つIssueとして登録（3分）

---

*i-kigai株式会社 / AI Workshop Series #3*
