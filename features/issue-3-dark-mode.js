/*
 * Issue #3：ダークモードを付ける
 * 担当：Cチーム
 * 指定ブランチ：feature/issue-3-dark-mode
 *
 * 完了条件：Issue本文の動作をブラウザで確認できること。
 * 編集範囲：features/issue-3-dark-mode.js
 * このファイルだけを編集してください。共通ファイルや他チームのファイルは変更しません。
 *
 * ヒント：切替ボタンを作り、app.mount('headerActions', 要素)で表示する。
 * クリック時にdocument.bodyのdark-modeクラスを切り替える。
 */
(function registerFeature(root) {
  root.WorkshopFeatures = root.WorkshopFeatures || {};
  root.WorkshopFeatures['issue-3'] = function setupIssue3(app) {
    // TODO：ここにIssue #3の実装を書く
  };
})(typeof window !== 'undefined' ? window : globalThis);
